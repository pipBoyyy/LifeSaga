<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifeSaga">  <!-- Это правильное объявление темы для application -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LifeSaga"> <!-- Тему можно указать и для Activity, или она унаследуется от application -->
            <!-- Если у вас есть intent-filter для MainActivity (чтобы она была запускаемой), он должен быть здесь -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Другие компоненты приложения, такие как сервисы, ресиверы или провайдеры, добавляются здесь, внутри <application> -->

    </application> <!-- Это единственный и правильный закрывающий тег для <application> -->

</manifest>